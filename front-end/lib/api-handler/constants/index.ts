export enum Http_Status {
  SUCCESS = 200,
  UNAUTHORIZED = 401,
  BAD_REQUEST = 404,
  INTERNAL_SERVER = 500,
  SERVICE_UNAVAILABLE = 503,
}

export enum NODE_ENV {
  PROD = "production",
  LOCAL = "local",
}
